# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'PatientDiary' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!
   pod 'GoogleSignIn'

  # Pods for PatientDiary

  target 'PatientDiaryTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'PatientDiaryUITests' do
    # Pods for testing
  end

end
